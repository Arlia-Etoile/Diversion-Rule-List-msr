# 基础配置
work_dir: "./tmp"
output_dir: "./out"

# Mihomo 配置
mihomo:
  api_url: "https://api.github.com/repos/MetaCubeX/mihomo/releases"
  start_with: "mihomo-linux-amd64-v"
  end_with: ".gz"

# Git 配置
git:
  user_email: "259484229+oom-wg[bot]@users.noreply.github.com"
  user_name: "oom-wg[bot]"
  release_branch: "mrs"
  max_history: 7
  timezone: "Asia/Taipei"

# 任务配置
tasks:
  AdBlock:
    format: "mrs"
    type: "domain"
    src: 
      - "https://rule.kelee.one/Clash/AdvertisingLite.yaml"
      - "https://raw.githubusercontent.com/Arlia-Etoile/Diversion-Rule-List/refs/heads/legacy/yaml/OtherBlock.yaml"
      - "https://rule.kelee.one/Clash/JiGuangTuiSong.yaml"
      - "https://rule.kelee.one/Clash/YouMengChuangXiang.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains

      # 替换与跳过逻辑
      rules_map = {
          ".xx.fbcdn.net.iberostar.com": "+.xx.fbcdn.net.iberostar.com",
          ".wns.windows.com":            "+.wns.windows.com",
          ".p2l.info":                   "+.p2l.info",
          ".wo.com.cn":                  None,  
          ".apps.iocnt.de":              "+.apps.iocnt.de" 
      }
      new_domains = []
      for d in domains:
          skip = False
          if d.startswith("+.data-") and d.endswith(".apps.iocnt.de"):
              skip = True
          if not skip:
              for suffix in rules_map:
                  if d.endswith(suffix):
                      skip = True
                      break
          if not skip:
              new_domains.append(d)

      for replacement in rules_map.values():
          if replacement:
              new_domains.append(replacement)
      domains = new_domains
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  AdBlock-IP:
    format: "mrs"
    type: "ipcidr"
    src:
      - "https://rule.kelee.one/Clash/AdvertisingLite.yaml"
      - "https://raw.githubusercontent.com/Arlia-Etoile/Diversion-Rule-List/refs/heads/legacy/yaml/OtherBlock.yaml"
      - "https://rule.kelee.one/Clash/JiGuangTuiSong.yaml "
      - "https://rule.kelee.one/Clash/YouMengChuangXiang.yaml"


  Privacy_Protection:
    format: "mrs"
    type: "domain"
    src: 
      - "https://ruleset.skk.moe/Clash/non_ip/sogouinput.txt"
      - "https://raw.githubusercontent.com/ConnersHua/RuleGo/master/Surge/Ruleset/Extra/Reject/Tracking.list"
      - "https://ruleset.skk.moe/Clash/non_ip/reject.txt"
      - "https://ruleset.skk.moe/Clash/ip/reject.txt"

    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains

      # 替换与跳过逻辑
      rules_map = {
          ".xx.fbcdn.net.iberostar.com": "+.xx.fbcdn.net.iberostar.com",
          ".wns.windows.com":            "+.wns.windows.com",
          ".p2l.info":                   "+.p2l.info",
          ".wo.com.cn":                  None,  
          ".apps.iocnt.de":              "+.apps.iocnt.de" 
      }
      new_domains = []
      for d in domains:
          skip = False
          if d.startswith("+.data-") and d.endswith(".apps.iocnt.de"):
              skip = True
          if not skip:
              for suffix in rules_map:
                  if d.endswith(suffix):
                      skip = True
                      break
          if not skip:
              new_domains.append(d)

      for replacement in rules_map.values():
          if replacement:
              new_domains.append(replacement)
      domains = new_domains
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  Privacy_Protection-IP:
    format: "mrs"
    type: "ipcidr"
    src:
      - "https://ruleset.skk.moe/Clash/non_ip/sogouinput.txt"
      - "https://raw.githubusercontent.com/ConnersHua/RuleGo/master/Surge/Ruleset/Extra/Reject/Tracking.list"
      - "https://ruleset.skk.moe/Clash/non_ip/reject.txt"
      
  360:
    format: "mrs"
    type: "domain"
    src: 
      - "https://rule.kelee.one/Clash/360.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  iCloudCN:
    format: "mrs"
    type: "domain"
    src: 
      - "https://raw.githubusercontent.com/Arlia-Etoile/Diversion-Rule-List/refs/heads/legacy/yaml/iCloud%20CN.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  iOS_Updates:
    format: "mrs"
    type: "domain"
    src: 
      - "https://raw.githubusercontent.com/Arlia-Etoile/Diversion-Rule-List/refs/heads/legacy/yaml/iOS_Updates.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  Direct:
    format: "mrs"
    type: "domain"
    src: 
      - "https://rule.kelee.one/Clash/Direct.yaml"
      - "https://kelee.one/Tool/Clash/Rule/ChinaDownloadCDN.yaml"
      - "https://rule.kelee.one/Clash/WeChat.yaml"
      - "https://rule.kelee.one/Clash/XiaoHongShu.yaml"
      - "https://raw.githubusercontent.com/Arlia-Etoile/Diversion-Rule-List/refs/heads/legacy/yaml/JailbreakRepoCN.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains

      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  Direct-IP:
    format: "mrs"
    type: "ipcidr"
    src:
      - "https://rule.kelee.one/Clash/Direct.yaml"
      - "https://kelee.one/Tool/Clash/Rule/ChinaDownloadCDN.yaml"
      - "https://rule.kelee.one/Clash/WeChat.yaml"
      - "https://raw.githubusercontent.com/Arlia-Etoile/Diversion-Rule-List/refs/heads/legacy/yaml/JailbreakRepoCN.yaml"
  
  Bank_CN:
    format: "mrs"
    type: "domain"
    src: 
      - "https://rule.kelee.one/Clash/ABC.yaml"
      - "https://rule.kelee.one/Clash/BOC.yaml"
      - "https://rule.kelee.one/Clash/BOCOM.yaml"
      - "https://rule.kelee.one/Clash/CCB.yaml"
      - "https://rule.kelee.one/Clash/CEB.yaml"
      - "https://rule.kelee.one/Clash/CGB.yaml"
      - "https://rule.kelee.one/Clash/CMB.yaml"
      - "https://rule.kelee.one/Clash/PSBC.yaml"
      - "https://rule.kelee.one/Clash/PingAn.yaml"
      - "https://rule.kelee.one/Clash/UnionPay.yaml"
      - "https://kelee.one/Tool/Clash/Rule/ChinaDownloadCDN.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains

      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  Bank_CN-IP:
    format: "mrs"
    type: "ipcidr"
    src:
      - "https://rule.kelee.one/Clash/ABC.yaml"
      - "https://rule.kelee.one/Clash/BOC.yaml"
      - "https://rule.kelee.one/Clash/BOCOM.yaml"
      - "https://rule.kelee.one/Clash/CCB.yaml"
      - "https://rule.kelee.one/Clash/CEB.yaml"
      - "https://rule.kelee.one/Clash/CGB.yaml"
      - "https://rule.kelee.one/Clash/CMB.yaml"
      - "https://rule.kelee.one/Clash/PSBC.yaml"
      - "https://rule.kelee.one/Clash/PingAn.yaml"
      - "https://rule.kelee.one/Clash/UnionPay.yaml"
      - "https://kelee.one/Tool/Clash/Rule/ChinaDownloadCDN.yaml"

  Proxy:
    format: "mrs"
    type: "domain"
    src: 
      - "https://kelee.one/Tool/Clash/Rule/Proxy.yaml"
      - "https://rule.kelee.one/Clash/Google.yaml"
      - "https://rule.kelee.one/Clash/Duckduckgo.yaml"
      - "https://rule.kelee.one/Clash/GitHub.yaml"
      - "https://rule.kelee.one/Clash/GitLab.yaml"
      - "https://rule.kelee.one/Clash/BBC.yaml"
      - "https://rule.kelee.one/Clash/NYTimes.yaml"
      - "https://rule.kelee.one/Clash/Bloomberg.yaml"
      - "https://rule.kelee.one/Clash/ThomsonReuters.yaml"
      - "https://rule.kelee.one/Clash/CNN.yaml"
      - "https://rule.kelee.one/Clash/BoXun.yaml"
      - "https://raw.githubusercontent.com/Arlia-Etoile/Diversion-Rule-List/refs/heads/legacy/yaml/JailbreakRepo.yaml"
      - "https://raw.githubusercontent.com/Arlia-Etoile/Diversion-Rule-List/refs/heads/legacy/yaml/Proxy%2B.yaml"
      - "https://rule.kelee.one/Clash/Notion.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  Proxy-IP:
    format: "mrs"
    type: "ipcidr"
    src:
      - "https://kelee.one/Tool/Clash/Rule/Proxy.yaml"
      - "https://rule.kelee.one/Clash/Telegram.yaml"
      - "https://rule.kelee.one/Clash/Line.yaml"
      - "https://rule.kelee.one/Clash/Google.yaml"
      - "https://rule.kelee.one/Clash/Duckduckgo.yaml"
      - "https://rule.kelee.one/Clash/GitHub.yaml"
      - "https://rule.kelee.one/Clash/GitLab.yaml"
      - "https://rule.kelee.one/Clash/BBC.yaml"
      - "https://rule.kelee.one/Clash/NYTimes.yaml"
      - "https://rule.kelee.one/Clash/Bloomberg.yaml"
      - "https://rule.kelee.one/Clash/ThomsonReuters.yaml"
      - "https://rule.kelee.one/Clash/CNN.yaml"
      - "https://rule.kelee.one/Clash/BoXun.yaml"
      - "https://raw.githubusercontent.com/Arlia-Etoile/Diversion-Rule-List/refs/heads/legacy/yaml/JailbreakRepo.yaml"
      - "https://raw.githubusercontent.com/Arlia-Etoile/Diversion-Rule-List/refs/heads/legacy/yaml/Proxy%2B.yaml"
      - "https://rule.kelee.one/Clash/Notion.yaml"

  Social:
    format: "mrs"
    type: "domain"
    src: 
      - "https://rule.kelee.one/Clash/Telegram.yaml"
      - "https://rule.kelee.one/Clash/Twitter.yaml"
      - "https://raw.githubusercontent.com/Arlia-Etoile/Diversion-Rule-List/refs/heads/legacy/yaml/Fedi.yaml"
      - "https://rule.kelee.one/Clash/Instagram.yaml"
      - "https://rule.kelee.one/Clash/Facebook.yaml"
      - "https://rule.kelee.one/Clash/Whatsapp.yaml"
      - "https://rule.kelee.one/Clash/Clubhouse.yaml"
      - "https://rule.kelee.one/Clash/Discord.yaml"
      - "https://rule.kelee.one/Clash/Snap.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  Social-IP:
    format: "mrs"
    type: "ipcidr"
    src:
      - "https://rule.kelee.one/Clash/Telegram.yaml"
      - "https://rule.kelee.one/Clash/Twitter.yaml"
      - "https://raw.githubusercontent.com/Arlia-Etoile/Diversion-Rule-List/refs/heads/legacy/yaml/Fedi.yaml"
      - "https://rule.kelee.one/Clash/Instagram.yaml"
      - "https://rule.kelee.one/Clash/Facebook.yaml"
      - "https://rule.kelee.one/Clash/Whatsapp.yaml"
      - "https://rule.kelee.one/Clash/Clubhouse.yaml"
      - "https://rule.kelee.one/Clash/Discord.yaml"
      - "https://rule.kelee.one/Clash/Snap.yaml"
  
  Pixiv:
    format: "mrs"
    type: "domain"
    src: 
      - "https://rule.kelee.one/Clash/Pixiv.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  YouTube:
    format: "mrs"
    type: "domain"
    src: 
      - "https://rule.kelee.one/Clash/YouTube.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  YouTube-IP:
    format: "mrs"
    type: "ipcidr"
    src:
      - "https://rule.kelee.one/Clash/YouTube.yaml"
      
  GamePF:
    format: "mrs"
    type: "domain"
    src: 
      - "https://rule.kelee.one/Clash/Epic.yaml"
      - "https://rule.kelee.one/Clash/Steam.yaml"
      - "https://kelee.one/Tool/Clash/Rule/InternationalDownloadCDN.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")
      
  GamePF-IP:
    format: "mrs"
    type: "ipcidr"
    src:
      - "https://rule.kelee.one/Clash/Epic.yaml"
      - "https://rule.kelee.one/Clash/Steam.yaml"
      - "https://kelee.one/Tool/Clash/Rule/InternationalDownloadCDN.yaml"

  Rockstar:
    format: "mrs"
    type: "domain"
    src: 
      - "https://rule.kelee.one/Clash/Rockstar.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  Hoyoverse:
    format: "mrs"
    type: "domain"
    src: 
      - "https://raw.githubusercontent.com/Arlia-Etoile/Diversion-Rule-List/refs/heads/legacy/yaml/Hoyoverse.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  AI:
    format: "mrs"
    type: "domain"
    src: 
      - "https://rule.kelee.one/Clash/OpenAI.yaml"
      - "https://rule.kelee.one/Clash/BardAI.yaml"
      - "https://rule.kelee.one/Clash/Gemini.yaml"
      - "https://rule.kelee.one/Clash/Copilot.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")
      
  AI-IP:
    format: "mrs"
    type: "ipcidr"
    src:
      - "https://rule.kelee.one/Clash/OpenAI.yaml"
      - "https://rule.kelee.one/Clash/BardAI.yaml"
      - "https://rule.kelee.one/Clash/Gemini.yaml"
      - "https://rule.kelee.one/Clash/Copilot.yaml"
      
  ClaudeAI:
    format: "mrs"
    type: "domain"
    src: 
      - "https://rule.kelee.one/Clash/Claude.yaml"
      - "https://rule.kelee.one/Clash/Anthropic.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  ClaudeAI-IP:
    format: "mrs"
    type: "ipcidr"
    src:
      - "https://rule.kelee.one/Clash/Claude.yaml"
      - "https://rule.kelee.one/Clash/Anthropic.yaml"

  Microsoft:
    format: "mrs"
    type: "domain"
    src: 
      - "https://rule.kelee.one/Clash/Microsoft.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  Microsoft-IP:
    format: "mrs"
    type: "ipcidr"
    src:
      - "https://rule.kelee.one/Clash/Microsoft.yaml"

  Onedrive:
    format: "mrs"
    type: "domain"
    src: 
      - "https://rule.kelee.one/Clash/OneDrive.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  Onedrive-IP:
    format: "mrs"
    type: "ipcidr"
    src:
      - "https://rule.kelee.one/Clash/OneDrive.yaml"
  
  PayPal:
    format: "mrs"
    type: "domain"
    src: 
      - "https://rule.kelee.one/Clash/PayPal.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  PayPal-IP:
    format: "mrs"
    type: "ipcidr"
    src:
      - "https://rule.kelee.one/Clash/PayPal.yaml"

  YouTubeMusic:
    format: "mrs"
    type: "domain"
    src: 
      - "https://rule.kelee.one/Clash/YouTubeMusic.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  YouTubeMusic-IP:
    format: "mrs"
    type: "ipcidr"
    src:
      - "https://rule.kelee.one/Clash/YouTubeMusic.yaml"

  Speedtest:
    format: "mrs"
    type: "domain"
    src: 
      - "https://rule.kelee.one/Clash/Speedtest.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  Speedtest-IP:
    format: "mrs"
    type: "ipcidr"
    src:
      - "https://rule.kelee.one/Clash/Speedtest.yaml"

  APNs:
    format: "mrs"
    type: "domain"
    src: 
      - "https://kelee.one/Tool/Clash/Rule/ApplePushNotificationService.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  APNs-IP:
    format: "mrs"
    type: "ipcidr"
    src:
      - "https://kelee.one/Tool/Clash/Rule/ApplePushNotificationService.yaml"

  AppleID:
    format: "mrs"
    type: "domain"
    src: 
      - "https://rule.kelee.one/Clash/AppleID.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  AppStore:
    format: "mrs"
    type: "domain"
    src: 
      - "https://rule.kelee.one/Clash/AppStore.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  Testflight:
    format: "mrs"
    type: "domain"
    src: 
      - "https://rule.kelee.one/Clash/TestFlight.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)
  iCloud:
    format: "mrs"
    type: "domain"
    src: 
      - "https://raw.githubusercontent.com/Arlia-Etoile/Diversion-Rule-List/refs/heads/legacy/yaml/iCloud.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  iCloud-IP:
    format: "mrs"
    type: "ipcidr"
    src:
      - "https://raw.githubusercontent.com/Arlia-Etoile/Diversion-Rule-List/refs/heads/legacy/yaml/iCloud.yaml"

  Apple:
    format: "mrs"
    type: "domain"
    src: 
      - "https://rule.kelee.one/Clash/Apple.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")

  Apple-IP:
    format: "mrs"
    type: "ipcidr"
    src:
      - "https://rule.kelee.one/Clash/Apple.yaml"

  Prevent_DNS_Leaks:
    format: "mrs"
    type: "domain"
    src: 
      - "https://raw.githubusercontent.com/Arlia-Etoile/Diversion-Rule-List/refs/heads/legacy/yaml/Prevent_DNS_Leaks.yaml"
      
    custom_script: |
      import re
      import ipaddress
      print(f"  -> 执行域名清洗，处理前数量: {len(domains)}")

      filtered_domains = []
      for d in domains:
          d_upper = d.upper()
          
          # 1. 将包含 IP 相关关键字直接踢出
          if "IP-CIDR" in d_upper or "IP-ASN" in d_upper or "GEOIP" in d_upper:
              continue
          
          # 2. 洗白 Classical 格式 (把 DOMAIN-SUFFIX,abc.com 变成 +.abc.com)
          if d_upper.startswith("DOMAIN-SUFFIX,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = "+." + parts[1].strip()
          elif d_upper.startswith("DOMAIN,"):
              parts = d.split(',')
              if len(parts) >= 2:
                  d = parts[1].strip()
          elif d_upper.startswith("DOMAIN-KEYWORD,"):
              continue # domain 规则集不支持 keyword，直接忽略
              
          # 3. 兜底：处理潜伏的纯 IP 和 IPv6
          clean_d = re.sub(r'^[\+\*\.\"\']+|[\"\']+$', '', d.strip())
          is_ip = False
          try:
              ipaddress.ip_network(clean_d, strict=False)
              is_ip = True
          except ValueError:
              pass
              
          if is_ip or ':' in clean_d:
              continue
              
          filtered_domains.append(d)

      domains = filtered_domains
      
      print(f"  -> 清洗完成，处理后数量: {len(domains)}")
      
